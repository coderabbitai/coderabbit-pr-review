# .semgrep.yml
rules:
  - id: hardcoded-password
    pattern: password = "$SECRET"
    message: "Avoid hardcoded passwords"
    severity: ERROR
    languages: [python]
    metadata:
      category: security

include:
  - semgrep/  # or even semgrep/example.py

